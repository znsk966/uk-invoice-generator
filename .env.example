# Copy this file to backend/.env (or export the variable) and adjust as needed.
# If your password contains URL-reserved characters (@ : / ? # [ ] etc.),
# URL-encode them in the value below.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/uk_invoice_dev
